# Nome da aplicação
spring.application.name=authService

# Configurações do banco de dados (exemplo PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/authdb
spring.datasource.username=seu_usuario
spring.datasource.password=sua_senha
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configurações do servidor
server.port=8080

# Logging
logging.level.org.springframework=INFO
logging.level.org.example=DEBUG

# JWT (exemplo de chave secreta)
jwt.secret=SuaChaveSecretaMuitoSeguraAqui1234567890

# Outras configurações que você pode precisar:
# spring.main.allow-bean-definition-overriding=true (se usar beans customizados)
# spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true (se precisar lazy loading)

